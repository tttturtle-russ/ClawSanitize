F2: Code Quality Review
Generated: 2026-03-02
Reviewer: Sisyphus-Junior (claude-sonnet-4.6)

═══════════════════════════════════════════════════════
AUTOMATED CHECKS
═══════════════════════════════════════════════════════

go build ./...
  EXIT: 0 — PASS

go vet ./...
  EXIT: 0 — PASS

go test ./... -count=1
  ok  github.com/yourusername/clawsanitizer/cmd                 0.098s
  ok  github.com/yourusername/clawsanitizer/internal/detectors  0.077s
  ok  github.com/yourusername/clawsanitizer/internal/parser     0.005s
  ok  github.com/yourusername/clawsanitizer/internal/scanner    0.100s
  ok  github.com/yourusername/clawsanitizer/internal/scoring    0.004s
  (no test files for: main, internal/api, internal/output, internal/types)
  TOTAL: 73 tests, 0 failures — PASS

TODO/FIXME/HACK/XXX grep: 0 matches — CLEAN
panic() grep: 0 matches — CLEAN

═══════════════════════════════════════════════════════
FILE-BY-FILE REVIEW (28 files)
═══════════════════════════════════════════════════════

--- main.go [CLEAN] ---
  7 lines. Thin entry point calling cmd.Execute(). Perfect.

--- cmd/root.go [CLEAN] ---
  17 lines. Cobra root command with proper os.Exit(1) on error. Clean.

--- cmd/scan.go [MINOR ISSUE] ---
  Issue 1: os.Exit() called inside cobra RunE — correct pattern for a CLI 
  tool but return nil after os.Exit(0) at line 84 is dead code. The 
  function will never reach `return nil` because os.Exit(0) terminates 
  the process. This is not a bug (cobra doesn't use the returned error 
  when os.Exit is called first) but is technically unreachable code.
  
  Issue 2: Comment style is over-annotated (lines 21-35 number each step 
  1-5 with comments "Path resolution precedence:"). The comments accurately
  describe the logic but border on explaining-the-obvious AI slop. 
  Acceptable given it's documenting precedence rules.
  
  Verdict: CLEAN (minor dead code at line 84, no functional issues)

--- internal/types/finding.go [CLEAN] ---
  Well-structured constants + struct. No issues.

--- internal/types/scan_result.go [CLEAN] ---
  Minimal, correct. No issues.

--- internal/types/config.go [CLEAN] ---
  Struct definitions match the JSON schema. No issues.

--- internal/scanner/orchestrator.go [CLEAN] ---
  Magic number: TotalChecks hardcoded as 23 at line 49. This should 
  be a constant (e.g., const totalChecks = 23) since the number is 
  meaningful (the documented check count). If checks are added/removed,
  this could silently drift. MINOR.
  
  Otherwise: clean orchestration flow, graceful degradation on workspace/
  MCP parse errors with log.Printf warnings (appropriate for non-fatal).

--- internal/scoring/score.go [CLEAN] ---
  Magic numbers (25, 10, 5, 1) for score deductions. However, these are
  documented in the function comment header with explicit per-severity 
  mapping, making the values self-explanatory in context. Acceptable.
  The INFO branch comment "no deduction" is slightly redundant but not slop.

--- internal/output/terminal.go [CLEAN] ---
  Color variable names (criticalColor, highColor, etc.) are clear.
  printFinding comments ("Print severity badge + title" etc.) are
  somewhat AI-sloppy — these are trivially obvious from the code.
  Minor but acceptable in output formatting code.

--- internal/output/json.go [CLEAN] ---
  Minimal, correct use of json.NewEncoder with error wrapping. Clean.

--- internal/parser/config.go [CLEAN] ---
  Proper error wrapping with fmt.Errorf + %w throughout. No issues.

--- internal/parser/workspace.go [CLEAN] ---
  readFileOrEmpty returns empty string on error — intentional and 
  documented. Clean nil handling.

--- internal/parser/mcp.go [ISSUE] ---
  Issue: Line 23 silently ignores os.UserHomeDir() error:
    `home, _ := os.UserHomeDir()`
  If this fails, `home` is empty string and filepath.Join will produce
  a path like "/workspace/TOOLS.md" — silently wrong. All other 
  callers (config.go, workspace.go) properly return the error.
  This is an **inconsistency** and a minor correctness issue.
  
  Otherwise clean. Regex-based markdown parsing is reasonable for the use case.

--- internal/api/clawhub.go [CLEAN] ---
  Magic string "https://api.clawhub.io/v1" at line 19. Should be a const.
  Magic number 5 * time.Second at line 20. Acceptable as it's a one-time
  default construction.
  
  Graceful offline fallback pattern (return nil, nil on network error) is
  well-designed and documented. Parse errors also fall back gracefully.
  
  Verdict: CLEAN (base URL should be const, minor)

--- internal/detectors/supply_chain.go [CLEAN] ---
  Good nil check on checkS4EmptyHashes (early return for empty skills).
  dangerousPatterns slice at line 94 is an unexported package-level var
  (inline literal) — fine for this size.
  Finding IDs use string literals "SUPPLY_CHAIN-001" etc. rather than
  typed constants — this is a minor inconsistency vs. the Severity/Category
  constants pattern, but not a bug.

--- internal/detectors/configuration.go [ISSUE] ---
  Issue: strings.Title() is deprecated since Go 1.18+ (line 181).
    Title: fmt.Sprintf("%s enabled without authentication", strings.Title(tunnelType))
  The `tunnelType` strings are already correctly cased at construction 
  time (lines 170-175), so strings.Title() is actually a no-op here — 
  the strings already start with capital letters ("Tailscale tunnel is", 
  "SSH tunnel is", etc.). This is dead/redundant code that uses a 
  deprecated function. Should use golang.org/x/text/cases or just remove
  the call since tunnelType is always pre-capitalized.
  
  Known from notepads: documented as known issue.

--- internal/detectors/discovery.go [CLEAN] ---
  Package-level regex vars (poisoningPatterns, etc.) compiled once at 
  init — correct pattern.
  truncate() helper is simple and correct. Not AI slop.
  Naming (D1-D6 methods) is consistent with spec.

--- internal/detectors/runtime.go [ISSUE] ---
  Issue: Line 57 sets FilePath to tool.Name instead of tool.Source:
    FilePath: tool.Name,
  In checkR1ForbiddenZoneAccess for tool matches (line 57), the FilePath
  field is set to tool.Name (the tool's name string like "danger-tool")
  instead of tool.Source (the actual file path). All other tool-based 
  findings in this file and in discovery.go use tool.Source. This looks
  like a copy-paste bug — tool.Name is not a file path.
  
  The check for workspace sources (line 87) correctly uses source.path.
  
  runtimeMobilePermissionPatterns uses struct literals with named fields —
  clean. The mobile audit dedup map (found map[string]string) is correct.

═══════════════════════════════════════════════════════
TEST FILE REVIEW (12 test files)
═══════════════════════════════════════════════════════

cmd/scan_test.go [CLEAN]
  Uses testify/assert. Integration-level tests. Well-structured.
  TestScan_FindingIDsUnique uses composite key (ID+FilePath) — correct 
  given duplicate IDs are allowed across different files.

internal/scanner/orchestrator_test.go [CLEAN]
  Uses stdlib testing (no testify). Consistent with package convention.
  Thorough field validation in TestScan_FindingsHaveRequiredFields.

internal/detectors/supply_chain_test.go [CLEAN]
  httptest server usage is correct. makeTestSupplyChainDetector helper 
  avoids repetition. Table-driven subtests for S4 keywords — good pattern.
  TestSupplyChain_S2_OfflineFallback makes real network call (slow).
  TestSupplyChain_S2_OfflineGracefulFallback correctly closes server first.

internal/detectors/configuration_test.go [CLEAN]
  TestConfiguration_EmptyConfig_HasExpectedFindings documents the 
  known Auth: false zero-value behavior — good regression test.

internal/detectors/discovery_test.go [CLEAN]
  TestDiscovery_NilWorkspace_WithTools covers the important D5 + D6 
  combination. Unicode homograph tests use actual Cyrillic characters.

internal/detectors/runtime_test.go [CLEAN]
  TestRuntime_R4_LoopbackNoFinding documents the intentional loopback
  exemption. Good edge case coverage.

internal/parser/config_test.go [MINOR]
  TestOpenClawConfig_Fields (lines 45-51) accesses fields with `_ =` to
  ensure they compile. This is a non-test — it verifies struct field 
  existence at compile time. Unusual pattern; could be a comment instead.
  Not harmful but adds noise.

internal/parser/workspace_test.go [CLEAN]
internal/parser/mcp_test.go [CLEAN]
internal/scoring/score_test.go [CLEAN]
  Full table-driven test covering boundary conditions (clamping to 0).

═══════════════════════════════════════════════════════
SUMMARY OF ISSUES FOUND
═══════════════════════════════════════════════════════

BUGS (functional correctness):
  [BUG-1] runtime.go:57 — FilePath set to tool.Name instead of tool.Source
    in checkR1ForbiddenZoneAccess for tool matches. tool.Name is not a 
    file path. Should be tool.Source.

DEPRECATION:
  [DEP-1] configuration.go:181 — strings.Title() deprecated since Go 1.18.
    Call is also a no-op (tunnelType already capitalized). Remove or replace.

SILENT ERROR DISCARD:
  [ERR-1] mcp.go:23 — os.UserHomeDir() error silently discarded with _.
    Inconsistent with all other callers (config.go, workspace.go).
    If home dir lookup fails, path is silently wrong.

DEAD CODE:
  [DEAD-1] scan.go:84 — `return nil` after os.Exit(0) is unreachable.

MAGIC NUMBERS/STRINGS:
  [MAGIC-1] orchestrator.go:49 — TotalChecks: 23 hardcoded literal.
    Should be a named constant.
  [MAGIC-2] clawhub.go:19 — BaseURL "https://api.clawhub.io/v1" is a 
    magic string, should be a package-level const.

AI SLOP:
  [SLOP-1] terminal.go:31-40 — inline comments ("Print severity badge",
    "Print ID and category", etc.) explain obvious single-line operations.
    Borderline but acceptable in output formatting code.
  [SLOP-2] scan.go:21-35 — numbered step comments (1-5) for path 
    resolution are documentation of precedence rules, not slop. OK.

NO ISSUES FOUND:
  - No TODO/FIXME/HACK/XXX comments
  - No panic() calls in any file
  - No generic names (data/result/item/temp) for important variables
  - No mixed camelCase/snake_case naming
  - No missing nil checks causing potential dereferences
  - Error wrapping with %w used consistently in all non-trivial errors
  - No unused imports or dead functions
  - Consistent use of types.Severity* and types.Category* constants
  - No over-engineering patterns

═══════════════════════════════════════════════════════
FILE STATUS
═══════════════════════════════════════════════════════

Files reviewed: 28
Clean (no issues): 22
Minor issues: 6
  - cmd/scan.go (dead code line 84)
  - internal/scanner/orchestrator.go (magic number 23)
  - internal/api/clawhub.go (magic string BaseURL)
  - internal/parser/mcp.go (silent error discard)
  - internal/detectors/configuration.go (deprecated strings.Title)
  - internal/detectors/runtime.go (BUG: FilePath = tool.Name)

═══════════════════════════════════════════════════════
VERDICT
═══════════════════════════════════════════════════════

Build [PASS] | Vet [PASS] | Tests [73 pass / 0 fail] | Files [22 clean / 6 issues]

VERDICT: APPROVE (with noted issues)

The codebase is well-structured, consistently styled, and all automated 
checks pass. The one functional bug (BUG-1: runtime.go FilePath=tool.Name) 
is low-severity — it produces a misleading FilePath in a finding but does 
not affect detection logic, scoring, or test results. The deprecated 
strings.Title call is also a no-op. No critical quality failures.
